import { sqliteTable, text, integer, primaryKey, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const players = sqliteTable('players', { id: text('id').primaryKey(), name: text('name').notNull(), code: text('code').notNull().unique() });
export const friendships = sqliteTable('friendships', { a: text('a').notNull().references(() => players.id), b: text('b').notNull().references(() => players.id), requester: text('requester').notNull(), accepted: integer('accepted').notNull().default(0) }, t => [primaryKey({ columns: [t.a, t.b] }), index('friendships_b').on(t.b)]);
export const rooms = sqliteTable('rooms', { code: text('code').primaryKey(), host: text('host').notNull().references(() => players.id), status: text('status').notNull().default('waiting'), round: integer('round').notNull().default(0), gameId: text('game_id'), created: integer('created').notNull() }, t => [index('rooms_host_created').on(t.host, t.created)]);
export const members = sqliteTable('members', { room: text('room').notNull().references(() => rooms.code), user: text('user').notNull().references(() => players.id) }, t => [primaryKey({ columns: [t.room, t.user] }), index('members_user').on(t.user)]);
export const games = sqliteTable('games', { id: text('id').primaryKey(), room: text('room').references(() => rooms.code), day: text('day'), answer: text('answer').notNull(), started: integer('started').notNull() }, t => [uniqueIndex('games_day').on(t.day)]);
export const progress = sqliteTable('progress', { game: text('game').notNull().references(() => games.id), user: text('user').notNull().references(() => players.id), rows: text('rows').notNull().default('[]'), attempts: integer('attempts').notNull().default(0), status: text('status').notNull().default('playing'), started: integer('started').notNull(), finished: integer('finished') }, t => [primaryKey({ columns: [t.game, t.user] }), index('progress_user_status').on(t.user, t.status)]);
